{
  "name": "ping-me",
  "version": "1.0.0",
  "description": "Keep your free tier backends alive with auto-pinging and monitoring",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "rm -rf node_modules && rm -rf **/node_modules && rm -rf **/dist",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "changeset": "changeset",
    "release": "turbo run build && changeset publish",
    "publish-packages": "npm run build && npm run publish-main && npm run publish-aliases",
    "publish-main": "cd packages/ping-me && npm publish --access public",
    "publish-aliases": "cd packages/keep-server-alive && npm publish --access public && cd ../keepwake && npm publish --access public && cd ../keepawake && npm publish --access public"
  },
  "keywords": [
    "ping",
    "uptime",
    "monitor",
    "keep-alive",
    "free-tier",
    "backend"
  ],
  "author": "Eshank Tyagi",
  "license": "MIT",
  "packageManager": "pnpm@10.12.1",
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "@eslint/eslintrc": "^3.3.1",
    "eslint": "^9.28.0",
    "prettier": "^3.5.3",
    "tsx": "^4.19.4",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3",
    "vitest": "^3.2.3"
  }
}